local startup = require("/csos/regimes/req-startup")
startup.Start()

startup.Finish()
startup.Auth()

term.reset()
advterm.Disable()
termTextColor = term.getTextColor()
term.setTextColor(colors.green)
print(startup.GetVersion() .. " #" .. os.getComputerID())
term.setTextColor(termTextColor)
